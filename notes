Generate APK android
- sudo keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload -storetype JKS
- cd android && ./gradlew clean && ./gradlew assembleRelease && ./gradlew bundleRelease
